log:
  stdout_log_level: INFO
  log_file_level: DEBUG
  log_file: "eng_product_agent.log"

apps:
  - name: "eng_product_agent"
    app_base_path: "."
    app_module: "solace_agent_mesh.agent.sac.app"
    broker:
      dev_mode: ${SOLACE_DEV_MODE, false}
      broker_url: ${SOLACE_BROKER_URL, ws://localhost:8080}
      broker_username: ${SOLACE_BROKER_USERNAME, default}
      broker_password: ${SOLACE_BROKER_PASSWORD, default}
      broker_vpn: ${SOLACE_BROKER_VPN, default}
      temporary_queue: ${USE_TEMPORARY_QUEUES, true}

    app_config:
      namespace: "${NAMESPACE}"
      supports_streaming: true
      agent_name: "Engineering Product Agent"
      display_name: "Engineering Product Agent"
      instruction: |
        You are Engineering Product Agent.
        Primary enterprise system: GitHub.

        Core responsibilities:
        - Provide PR review assistance with focused quality and risk comments.
        - Triage issues by severity, ownership, and customer impact.
        - Draft release notes from merged changes and linked tickets.
        - Assist CI or CD failure triage with likely root causes and next checks.

        Operating rules:
        - Use connected enterprise tools and uploaded artifacts first.
        - If the required GitHub connector is unavailable, explicitly state the missing integration and proceed with user-provided data only.
        - Do not fabricate records, KPIs, IDs, approvals, or outcomes.
        - Return actionable outputs with owners, due dates, assumptions, and risk level.

      model:
        model: "${LLM_SERVICE_GENERAL_MODEL_NAME}"
        api_base: "${LLM_SERVICE_ENDPOINT}"
        api_key: "${LLM_SERVICE_API_KEY}"

      tools:
        - tool_type: builtin-group
          group_name: "data_analysis"
        - tool_type: builtin-group
          group_name: "web"

      session_service:
        type: ${PERSISTENCE_TYPE, sql}
        database_url: ${DATABASE_URL, sqlite:///eng_product_agent.db}
        default_behavior: PERSISTENT

      artifact_service:
        type: ${ARTIFACT_SERVICE_TYPE, s3}
        bucket_name: ${S3_BUCKET_NAME}
        endpoint_url: ${S3_ENDPOINT_URL}
        aws_region: ${AWS_REGION, us-east-1}
        artifact_scope: namespace

      artifact_handling_mode: reference
      enable_embed_resolution: true
      enable_artifact_content_instruction: true

      agent_card:
        description: "Supports software delivery workflows from PR review through incident triage."
        defaultInputModes: ["text", "file"]
        defaultOutputModes: ["text", "file"]
        skills:
          - id: "engineering_delivery"
            name: "Engineering Delivery"
            description: "Supports software delivery workflows from PR review through incident triage."

      agent_card_publishing:
        interval_seconds: 15

      agent_discovery:
        enabled: false

      inter_agent_communication:
        allow_list: []
        request_timeout_seconds: 30
